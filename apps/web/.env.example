# Browser-visible API base URL
NEXT_PUBLIC_API_URL=http://localhost:4000

# Server-only (fake-pay signing route) — must match the API's WEBHOOK_SECRET
API_URL=http://localhost:4000
WEBHOOK_SECRET=change-me-too
